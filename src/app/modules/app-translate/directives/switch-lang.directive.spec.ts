import { SwitchLangDirective } from './switch-lang.directive';

describe('SwitchLangDirective', () => {
  it('should create an instance', () => {
    const directive = new SwitchLangDirective();
    expect(directive).toBeTruthy();
  });
});
